<h1 class="center">The Deceased</h3>
<br />
  <div class="row">
        <div class="col s12">
          <div class="z-depth-5">
            <div class="card">
              <div class="card-content">
                <span class="card-title black-text"><%= @deceased.birthdate %>___________________
                                                    <%= @deceased.deathdate %> 
                </span><br />
                <span class="card-title black-text"><%= @deceased.full_name %></span><br />
                <span class="card-title black-text"><h3>Died Doing What They Loved</h3></span>
                <span class="card-title black-text"><%= @deceased.cod %>
              </div>
            </div>
          </div>
  </div>
   <%= link_to 'Edit', edit_mortuary_deceased_path(@mortuary, @deceased), class: 'btn' %>
   <%= link_to 'Delete', mortuary_deceased_path(@mortuary, @deceased), method: :delete, 
                    data: {confirm: 'Really Delete?'}, class: 'btn orange white-text' %>
</div>

<%= link_to 'Deceased Home', mortuary_deceaseds_path(@mortuary) %>